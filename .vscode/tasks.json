{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Program the RP2040",
      "type": "shell",
      "command": "${workspaceFolder}/scripts/program.sh",
      "group": "build",
      "args": [
        "${command:cmake.launchTargetPath}"
      ],
      "presentation": {
        "reveal": "always",
        "panel": "new",
        "close": true
      }
    }
  ]
}